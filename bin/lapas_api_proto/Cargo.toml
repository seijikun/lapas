[package]
name = "lapas-api-proto"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thiserror = "1"
paste = "1"
async-trait = "0"
chrono = "0"
tokio = { version = "1", features = ["io-util", "sync"] }